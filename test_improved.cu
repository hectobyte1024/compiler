#include <cuda_runtime.h>
#include <device_launch_parameters.h>

// CUDA Kernel: test
__global__ void test_kernel(float* vertices, float* output, int numVertices) {
__device__ void main(    ) {
        x = vec4(1.0, 2.0, 3.0, 4.0);
    }
    
}

